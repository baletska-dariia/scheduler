import { DeadlinePipe } from './deadline-pipe.pipe';

describe('DeadlinePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DeadlinePipe();
    expect(pipe).toBeTruthy();
  });
});
